# Future Support: Chrome

Chrome is not a shipping target in this release.

Current status:

- Chromium manifest overlay exists in `manifests/chromium.json`.
- Core wiki behavior is implemented in shared code.

When Chrome shipping starts, validate permissions, store policy compliance, and full allowlist behavior before release.
