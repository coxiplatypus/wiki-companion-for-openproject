# Future Support: Safari

Safari is not a shipping target in this release.

Current status:

- Shared wiki logic is browser-agnostic.
- Safari packaging and distribution are not yet configured.

When Safari shipping starts, convert/package with Apple tooling, validate permissions flow, and run full manual QA.
